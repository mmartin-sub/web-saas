"use client";

import React from "react";

export default function PrivaacyPage() {
    // const [isLoading, setIsLoading] = React.useState<boolean>(false);


    return (
      <div className="container flex h-screen w-screen flex-col items-center justify-center">
    Test Privacy 2
      </div>
    );
  }
